slick.dbs {
  enrolmentstore {
    profile="slick.jdbc.H2Profile$"
    db.driver="org.h2.Driver"
    db.url="jdbc:h2:mem:enrolment-store;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false"
    db.username="sa"
    db.password=""
  }
}
